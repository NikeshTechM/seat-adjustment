version: 0.2

env:
  variables:
    IMAGE_REPO_NAME: "sdv-devops-engine/seat-adjustment"
    AWS_DEFAULT_REGION: "us-east-1"
    DDB_TABLE: "sdvDevopsPipelineDashboardDDTable"
    PIPELINE_EXECUTION_ID: "#{codepipeline.PipelineExecutionId}"

  secrets-manager:
    DOCKERHUB_USERNAME: dockerhub-credentials:username
    DOCKERHUB_PASSWORD: dockerhub-credentials:password

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Checking AWS CLI..."
      - aws --version || (
          curl "https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip" -o awscliv2.zip &&
          unzip -q awscliv2.zip &&
          ./aws/install --update
        )

      - echo "Installing Podman..."
      - yum install -y podman

  pre_build:
    commands:
      - echo "Login to DockerHub"
      - echo "$DOCKERHUB_PASSWORD" | podman login docker.io --username "$DOCKERHUB_USERNAME" --password-stdin

      - echo "Login to ECR"
      - AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      - ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"
      - aws ecr get-login-password --region "$AWS_DEFAULT_REGION" | podman login --username AWS --password-stdin "$ECR_REGISTRY"

      - BUILD_TIMESTAMP=$(date -u +"%Y%m%d%H%M%S")
      - IMAGE_TAG="$BUILD_TIMESTAMP"
      - FULL_IMAGE_URI="${ECR_REGISTRY}/${IMAGE_REPO_NAME}:${IMAGE_TAG}"
      - DOCKERHUB_IMAGE_URI="docker.io/${DOCKERHUB_USERNAME}/${IMAGE_REPO_NAME}:${IMAGE_TAG}"

  build:
    commands:
      - echo "Listing build context"
      - ls -la

      - echo "Building PROD image..."
      - podman build -f Dockerfile.prod -t "$FULL_IMAGE_URI" .

      # 🔹 BUILD STAGE UPDATE
      - |
        aws dynamodb update-item \
          --table-name "$DDB_TABLE" \
          --key '{ "ddid": { "S": "'"$PIPELINE_EXECUTION_ID"'" } }' \
          --update-expression "
            SET
              BuildContainerStage = :msg,
              BuildContainerStageStatus = :status,
              BuildContainerStageTimestamp = :ts
          " \
          --expression-attribute-values '{
            ":msg": { "S": "Successfully built image '"$FULL_IMAGE_URI"'" },
            ":status": { "S": "Yes" },
            ":ts": { "S": "'"$(date -u +"%Y-%m-%dT%H:%M:%SZ")"'" }
          }'

  post_build:
    commands:
      - echo "Building TEST image..."
      - podman build -f Dockerfile.test -t test-image:"$IMAGE_TAG" .

      - echo "Running tests (30s timeout)..."
      - timeout 30 podman run --rm test-image:"$IMAGE_TAG"

      # 🔹 TEST STAGE UPDATE
      - |
        aws dynamodb update-item \
          --table-name "$DDB_TABLE" \
          --key '{ "ddid": { "S": "'"$PIPELINE_EXECUTION_ID"'" } }' \
          --update-expression "
            SET
              TestAndValidateStage = :msg,
              TestAndValidateStageStatus = :status,
              TestAndValidateStageTimestamp = :ts
          " \
          --expression-attribute-values '{
            ":msg": { "S": "Testing Completed" },
            ":status": { "S": "Yes" },
            ":ts": { "S": "'"$(date -u +"%Y-%m-%dT%H:%M:%SZ")"'" }
          }'

      - echo "Tagging image for DockerHub..."
      - podman tag "$FULL_IMAGE_URI" "$DOCKERHUB_IMAGE_URI"

      - echo "Pushing image to ECR..."
      - podman push "$FULL_IMAGE_URI"

      - echo "Pushing image to DockerHub..."
      - podman push "$DOCKERHUB_IMAGE_URI"

      # 🔹 PUSH STAGE UPDATE
      - |
        aws dynamodb update-item \
          --table-name "$DDB_TABLE" \
          --key '{ "ddid": { "S": "'"$PIPELINE_EXECUTION_ID"'" } }' \
          --update-expression "
            SET
              PushToQuayStage = :pushmsg,
              PushToQuayStageTimestamp = :ts
          " \
          --expression-attribute-values '{
            ":pushmsg": { "S": "Push Completed" },
            ":ts": { "S": "'"$(date -u +"%Y-%m-%dT%H:%M:%SZ")"'" }
          }'

artifacts:
  files:
    - Dockerfile.prod
    - Dockerfile.test
  name: build-artifacts-${CODEBUILD_BUILD_ID}
  discard-paths: yes
